{
  "name": "pact-directory",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=packages/pact-data-model && npm run build --workspace=packages/pact-api-client && npm run build --workspace=apps/api",
    "test": "npm run test --workspaces --if-present"
  }
}
