# Include platform-specific optional deps for all supported platforms
# so that package-lock.json generated on macOS also contains the Linux
# binaries needed in CI (fixes @rollup/rollup-linux-x64-gnu missing error).
os[]=linux
os[]=darwin
cpu[]=x64
cpu[]=arm64
