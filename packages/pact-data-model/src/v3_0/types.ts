// This file is auto-generated by scripts/generate-types.ts. Do not edit manually.

export enum ProductFootprintStatus {
  Active = "Active",
  Deprecated = "Deprecated",
}

export enum CarbonFootprintDeclaredUnitOfMeasurement {
  Liter = "liter",
  Kilogram = "kilogram",
  CubicMeter = "cubic meter",
  KilowattHour = "kilowatt hour",
  Megajoule = "megajoule",
  TonKilometer = "ton kilometer",
  SquareMeter = "square meter",
  Piece = "piece",
  Hour = "hour",
  MegabitSecond = "megabit second",
}

export enum CarbonFootprintGeographyRegionOrSubregion {
  Africa = "Africa",
  Americas = "Americas",
  Asia = "Asia",
  Europe = "Europe",
  Oceania = "Oceania",
  AustraliaAndNewZealand = "Australia and New Zealand",
  CentralAsia = "Central Asia",
  EasternAsia = "Eastern Asia",
  EasternEurope = "Eastern Europe",
  LatinAmericaAndTheCaribbean = "Latin America and the Caribbean",
  Melanesia = "Melanesia",
  Micronesia = "Micronesia",
  NorthernAfrica = "Northern Africa",
  NorthernAmerica = "Northern America",
  NorthernEurope = "Northern Europe",
  Polynesia = "Polynesia",
  SouthEasternAsia = "South-eastern Asia",
  SouthernAsia = "Southern Asia",
  SouthernEurope = "Southern Europe",
  SubSaharanAfrica = "Sub-Saharan Africa",
  WesternAsia = "Western Asia",
  WesternEurope = "Western Europe",
}

export enum CarbonFootprintCcuCalculationApproach {
  CutOff = "Cut-off",
  Credit = "Credit",
}

export enum ProductOrSectorSpecificRuleOperator {
  PEF = "PEF",
  EPDInternational = "EPD International",
  Other = "Other",
}

export enum VerificationCoverage {
  PCFCalculationModel = "PCF calculation model",
  PCFProgram = "PCF program",
  ProductLevel = "product level",
}

export enum ErrorCode {
  BadRequest = "BadRequest",
  AccessDenied = "AccessDenied",
  TokenExpired = "TokenExpired",
  NotFound = "NotFound",
  InternalError = "InternalError",
  NotImplemented = "NotImplemented",
}

export type Urn = string;

/** `ProductFootprint` is a data type which represents the carbon footprint of a product under a specific scope ([[#dt-carbonfootprint-scope]]) and with values calculated in accordance with the [=PACT Methodology=]. The objective of a `ProductFootprint` is to provide interoperability between the creator (the [=data owner=]) and the consumer (the [=data recipient=]) of ProductFootprints. The details on the exchange of ProductFootprints are specified in [[#api]]. Conceptually, the data type <{ProductFootprint}> is modeled as a multi-purpose container for product-specific emission factors which is supported by extensibility through [=Data Model Extensions=]. Data Model Extensions enable [=data owners=] to exchange additional information related to a product with [=data recipients=]. The details are specified in [[#datamodelextension]] as well as [[!EXTENSIONS-GUIDANCE]], and [[!DATA-MODEL-EXTENSIONS]]. Each `ProductFootprint` can and should be updated over time, for instance to incorporate new or refined data from [=data owners=] (see [[#lifecycle]]). */
export interface ProductFootprint {
  /** A unique identifier that a system uses to refer to the entire dataset of the PCF. This is typically an automatically-generated number by the solution to maintain the required technical references to data within the system. */
  id: string;
  /** The version of the PACT Technical Specifications that the data being shared complies with. This is a string in the format of "major.minor.patch" (e.g. "3.0.0"). */
  specVersion: string;
  /** A given PCF may change over time, due to updates to the calculation. This is a list of IDs that reflect "past versions" of the current PCF, maintained by the solution. If defined, MUST be non-empty set of IDs. See [[#lifecycle]] for details. */
  precedingPfIds?: string[];
  /** The date and time when the PCF was created. This is typically an automatically generated field by the solution. It SHOULD NOT be used to derive status of validity of the PCF. See [[#lifecycle]] for details. */
  created: string;
  /** The status of the PCF. `Active` means that the PCF is the most recent version and is the one that SHOULD be used by a data recipient, e.g. for product footprint calculations. `Deprecated` means that the PCF is no longer the most recent version and SHOULD NOT be used by data recipients. See [[#lifecycle]] for details. */
  status: ProductFootprintStatus;
  /** The start date of the validity period: the time interval during which the ProductFootprint is declared as valid for use by a receiving [=data recipient=]. If no validity period is specified, the ProductFootprint is valid for 3 years after the <{CarbonFootprint/referencePeriodEnd}> See [[#validity-period]] for details. */
  validityPeriodStart?: string;
  /** The end date and time of the validity period. After this date the ProductFootprint is not valid for use anymore. See [[#validity-period]] for more details. */
  validityPeriodEnd?: string;
  /** The name of the company that is the PCF Data Owner */
  companyName: string;
  /** The non-empty set of Uniform Resource Names ([[!RFC8141|URN]]). Each value of this set is supposed to uniquely identify the ProductFootprint Data Owner. */
  companyIds: Urn[];
  /** The free-form description of the product, including any additional relevant information such as production technology, packaging, process, feedstock and technical parameters (e.g. dimensions). Products which are services (i.e. consulting) should include a short description of the service. */
  productDescription: string;
  /** The non-empty set of Product IDs in [[!RFC8141|URN]] format. Each of the values in the set is supposed to uniquely identify the product. See [[#product-identifier-urns]] for syntax and examples. */
  productIds: Urn[];
  /** The non-empty set of Product Classifications in [[!RFC8141|URN]] format. Each of the values in the set can classify the product as part of distinct groupings and categorizations. See [[#product-classification-urns]]. */
  productClassifications?: Urn[];
  /** The name with which the company producing the product refers to it, i.e. the product's trade name. Recognizable by the receiver of the PCF information. */
  productNameCompany: string;
  /** Any additional information related to the PCF. Whereas the property productDescription contains product-level information, comment should be used for information and instructions related to the calculation of the PCF, or other information which informs the ability to interpret (e.g. LUC not included as unable to calculate LUC), to audit, or to verify the PCF. Information explaining the current status of the PCF, what was changed since the last version, etc. If the PCF was changed since a previous version, indicate all methodological and/or production process change(s) that occurred to result in the PCF change. For example, include the relevant change(s) from the list below: 1. In case product or sector specific guidance used does not align with PACT Methodology's requirement, the areas of disalignment should be specified in the comment section (e.g. allocation rules, exemption rules, data quality metrics). 2. Information explaining the current status of the PCF, what was changed since the last version, etc. If the PCF was changed since a previous version, indicate all methodological and/or production process change(s) that occurred to result in the PCF change. E.g., include the relevant change(s) from the list below: Methodological: - Access to new Emission Factor data (database, supplier-specific, company-specific) - Updated upstream data (i.e. upstream supplier updated their PCF based on methodology change) Production Process: - Change in process - Change in feedstock - Change from conventional to certified sustainable material - Change in energy source - Change in upstream supplier - Updated upstream data (i.e. upstream supplier updated their PCF based on process change) 3. Additional information on biogenic emissions & removals calculation should be specified. This includes information on tools used for calculations (e.g. Cool Farm Tool), and methodological choices made in calculation of biogenic emissions and removals (e.g. Statistical or Direct Land use change calculation for DLUC calculations). */
  comment?: string;
  /** The carbon footprint of the given product with value conforming to the data type <{CarbonFootprint}>. */
  pcf: CarbonFootprint;
  /** If defined, 1 or more data model extensions associated with the ProductFootprint. See <{DataModelExtension}> for details. */
  extensions?: DataModelExtension[];
}

/** A CarbonFootprint represents the carbon footprint of a product and related data in accordance with the [=PACT Methodology=]. ### Scope of a CarbonFootprint ### {#dt-carbonfootprint-scope} Each CarbonFootprint is scoped by 1. Time Period: the time period is defined by the properties <{CarbonFootprint/referencePeriodStart}> and <{CarbonFootprint/referencePeriodEnd}> (see [=PACT Methodology=] section 3.2.3) 2. Geography: further set by the properties <{CarbonFootprint/geographyRegionOrSubregion}>, <{CarbonFootprint/geographyCountry}>, and <{CarbonFootprint/geographyCountrySubdivision}> (see [=PACT Methodology=] section 3.2.3) If a CarbonFootprint 1. Has geographical granularity `Global`, then the properties <{CarbonFootprint/geographyCountry}> and <{CarbonFootprint/geographyRegionOrSubregion}> and <{CarbonFootprint/geographyCountrySubdivision}> MUST be `undefined`; 2. Has a regional or sub-regional geographical granularity, then the property <{CarbonFootprint/geographyRegionOrSubregion}> MUST be `defined` and the properties <{CarbonFootprint/geographyCountry}> and <{CarbonFootprint/geographyCountrySubdivision}> MUST be `undefined`; 3. Has a country-specific geographical granularity, then property <{CarbonFootprint/geographyCountry}> MUST be `defined` AND the properties <{CarbonFootprint/geographyRegionOrSubregion}> and <{CarbonFootprint/geographyCountrySubdivision}> MUST be `undefined`; 4. Has a country subdivision-specific geographical granularity, then property <{CarbonFootprint/geographyCountrySubdivision}> MUST be `defined` AND the properties <{CarbonFootprint/geographyRegionOrSubregion}> and <{CarbonFootprint/geographyCountry}> MUST be `undefined`. */
export interface CarbonFootprint {
  /** The unit of measurement of the product. Together with `declaredUnitAmount` this defines the 'declared unit' of the product. Emissions in this carbon footprint are expressed in kgCO2e per 'declared unit'. For example: a PCF for a 12.5 liter bottle of Ethanol states 2 kg of CO2e in emissions. In this case the declared unit is 12.5 liter Ethanol, thus the `declaredUnitOfMeasurement` is "liter", and the `declaredUnitAmount` is "12.5". The `pcfIncludingBiogenicUptake` is "2.0" kgCO2e per "12.5 liter" of Ethanol. */
  declaredUnitOfMeasurement: CarbonFootprintDeclaredUnitOfMeasurement;
  /** The amount of <{CarbonFootprint/declaredUnitOfMeasurement|units}> contained within the product to which the [[#carbonfootprint|PCF]] is referring. For example: if the product is a car door weighing 80kg, `declaredUnitAmount` will be `80` and `declaredUnitOfMeasurement` will be `kilogram`. */
  declaredUnitAmount: string;
  /** The mass (in kg) of the product excluding packaging. The 'declared unit' is the `declaredUnitAmount` times `declaredUnitOfMeasurement`. For example, if the declared unit is `piece`, this attribute MUST be populated with the mass of `declaredUnitAmount` pieces of the product. If the declared unit is `liter`, this attribute MUST be populated with the mass of `declaredUnitAmount` liters of the product. If the product mass is not relevant (i.e. PCF is for an energy (kWh, MJ), logistics (ton.km) or service related product), this attribute SHALL be populated with `0`. For the full list of declared units requiring to report a mass per declared unit attribute please refer to table 4 in the PACT Methodology. */
  productMassPerDeclaredUnit: string;
  /** The start (inclusive) of the time boundary for which the PCF value is considered to be representative. Specifically, this start date represents the earliest date from which activity data was collected to include in the PCF calculation. */
  referencePeriodStart: string;
  /** The end (exclusive) of the time boundary for which the PCF value is considered to be representative. Specifically, this end date represents the latest date from which activity data was collected to include in the PCF calculation. */
  referencePeriodEnd: string;
  /** If present, the value MUST be one of the [=UN geographic regions=] or [=UN geographic subregions=]. See [[#dt-carbonfootprint-scope]] for further details. Additionally, see the [=PACT Methodology=] Section 3.2.3. */
  geographyRegionOrSubregion?: CarbonFootprintGeographyRegionOrSubregion;
  /** If present, the value MUST conform to the [ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code. See [[#dt-carbonfootprint-scope]] for further details. */
  geographyCountry?: string;
  /** If present, a [ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2) country and subdivision code. See [[#dt-carbonfootprint-scope]] for further details. */
  geographyCountrySubdivision?: string;
  /** Brief description of the processes attributable to each life cycle stage included in the PCF (e.g. electricity consumption for manufacturing), especially those that significantly contribute manufacturing steps of the product (including general description of used technologies). */
  boundaryProcessesDescription?: string;
  /** The PCF of the product: Including: - All fossil emissions (CO2, CH4, N2O, HFCs, SF6, NF3, PFCs, HFEs, PFPEs, CFCs and HCFSs) from stationary/mobile combustion, industrial processes and fugitive emissions - All land sector-related related emissions (CO2, N2O, PFCs) - All biogenic emissions (biogenic CH4, biogenic CO2) - Land management removals and technological removals Excluding: - Biogenic Product CO2 uptake */
  pcfExcludingBiogenicUptake: string;
  /** The PCF of the product: Including: - All fossil emissions (CO2, CH4, N2O, HFCs, SF6, NF3, PFCs, HFEs, PFPEs, CFCs and HCFSs) from stationary/mobile combustion, industrial processes and fugitive emissions - All land sector-related related emissions (CO2, N2O, PFCs) - All biogenic emissions (biogenic CH4, biogenic CO2) - Land management removals and technological removals - Biogenic Product CO2 uptake */
  pcfIncludingBiogenicUptake: string;
  /** The fossil carbon content of the product (mass of carbon). */
  fossilCarbonContent: string;
  /** The biogenic carbon content of the product (mass of carbon). */
  biogenicCarbonContent?: string;
  /** The carbon content (both biogenic and fossil) from recycled material in the product (mass of carbon). */
  recycledCarbonContent?: string;
  /** The emissions from fossil sources as a result of fuel combustion, from fugitive emissions, and from process emissions. Expressed in kgCO2e per declared unit. */
  fossilGhgEmissions: string;
  /** GHG emissions from land-use change, such as deforestation or conversion from natural forest to plantation forest, that cause carbon stock loss. */
  landUseChangeGhgEmissions?: string;
  /** Placeholder for indirect land use change causing displacement of food production, outside of value-chain */
  landCarbonLeakage?: string;
  /** Fossil CO2, N2O, fossil CH4, HFCs and PFCs emissions due to land management practices. Note: `fossilGhgEmissions` already includes these emissions. */
  landManagementFossilGhgEmissions?: string;
  /** Biogenic CO2 emissions occurring due to recurring land management actions on land within the same land-use category. */
  landManagementBiogenicCO2Emissions?: string;
  /** Biogenic CO2 removals resulting from a net increase in carbon stored in land-based carbon pools (e.g. reforestation and afforestation). Subject to traceability requirements. */
  landManagementBiogenicCO2Removals?: string;
  /** Temporary CO2 uptake of biomass in the product at point of leaving factory gate. */
  biogenicCO2Uptake?: string;
  /** CH4 emissions from land management practices and the oxidation and transformation or degradation of biomass. */
  biogenicNonCO2Emissions?: string;
  /** The amount of agricultural land occupied in the reporting year to produce the product. */
  landAreaOccupation?: string;
  /** If present, the GHG emissions resulting from aircraft engine usage for the transport of the product, excluding radiative forcing. The aircraft emissions are excluding biogenic CO2 uptake. */
  aircraftGhgEmissions?: string;
  /** Indicates whether packaging emissions are included in the scope and boundary of the product carbon footprint. If `true`, packaging emissions are included in the product carbon footprint, and the `packagingGhgEmissions` property SHOULD be defined. If `false`, packaging emissions are not included in the product carbon footprint, and the `packagingGhgEmissions` property MUST be undefined. */
  packagingEmissionsIncluded?: boolean;
  /** Emissions resulting from the packaging of the product. SHOULD be defined if `packagingEmissionsIncluded` is true and MUST be undefined if `packagingEmissionsIncluded` is false. Packaging emissions are excluding biogenic CO2 uptake. */
  packagingGhgEmissions?: string;
  /** The biogenic carbon content of the packaging (mass of carbon). */
  packagingBiogenicCarbonContent?: string;
  /** Emissions resulting from outbound logistics should be calculated and reported separately up to the point where another company (e.g., customer) takes over responsibility for the product (i.e. own or pay for the outbound logistics). MUST be undefined if the company calculating and exchanging the PCF is not responsible for the outbound logistics. The outbound logistics emissions are excluding biogenic CO2 uptake. */
  outboundLogisticsGhgEmissions?: string;
  /** Indicates whether CCS (including BECCS) take place within the scope and boundary of the product carbon footprint. If `true`, `ccsTechnologicalCO2Capture`, `technologicalCO2Removals` and `technologicalCO2CaptureOrigin` shall be defined if known and available. If `false`, `ccsTechnologicalCO2Capture`, `technologicalCO2Removals` and `technologicalCO2CaptureOrigin` shall be undefined. */
  ccsTechnologicalCO2CaptureIncluded?: boolean;
  /** The amount of CO2 captured with Carbon Capture and Storage (CCS) technologies. */
  ccsTechnologicalCO2Capture?: string;
  /** For CCU: Information about the origin (fossil or biogenic) and path of the captured CO2 used in CCU, including the name and location of the capture facility. This information enhances transparency and traceability, enabling tracking of CO2 across the value chain. For CCS: Traceability data, i.e. information on location injection site, geological reservoir as part of the overall technological CO2 capture origin data point for the PCF. */
  technologicalCO2CaptureOrigin?: string;
  /** CO2 removed directly from the atmosphere or via biogenic CO2 capture. Subject to reporting requirements. */
  technologicalCO2Removals?: string;
  /** The amount of captured carbon (both biogenic and fossil) during CCU (Carbon Capture & Usage) in the product. */
  ccuCarbonContent?: string;
  /** The calculation approach for CCU: "Cut-off" or "Credit." */
  ccuCalculationApproach?: CarbonFootprintCcuCalculationApproach;
  /** (Only for Credit Approach) a URL to documentation verifying the certification from an external bookkeeping scheme. This attribute ensures reliability and avoids double counting of credits within the crediting system. */
  ccuCreditCertification?: string;
  /** The characterization factors from one or more IPCC Assessment Reports used in the calculation of the PCF. It MUST be a non-empty set of strings with the format `AR$VERSION$`, where `$VERSION$` stands for the IPCC report version number and MUST be an integer. Per the Methodology the latest available characterization factor version shall be used, i.e., `["AR6"]`. In the event this is not possible, include the set of all characterization factors used. */
  ipccCharacterizationFactors: string[];
  /** The cross-sectoral standards applied for calculating or allocating [=GHG=] emissions. It MUST be a non-empty array and SHOULD contain only the following values without duplicates: : `ISO14067` :: for the ISO 14067 Standard, "Greenhouse gases — Carbon footprint of products — Requirements and guidelines for quantification" : `ISO14083` :: for the ISO 14083 Standard, "Greenhouse gases — Quantification and reporting of greenhouse gas emissions arising from transport chain operations" : `ISO14040-44` :: for the ISO 14040-44 Standard, "Environmental management — Life cycle assessment — Principles and framework" : `GHGP-Product` :: for the Greehouse Gas Protocol (GHGP) Product Standard : `PEF` :: for the EU Product Environmental Footprint Guide : `PACT-1.0` : `PACT-2.0` : `PACT-3.0` :: for a given version of the [=PACT Methodology=]. It is recommended to use the latest version of the Methodology. : `PAS2050` :: for the Publicly Available Specification (PAS) 2050, "Specification for the assessment of the life cycle greenhouse gas emissions of goods and services". The use of this standard is permitted but not recommended. The enumeration of standards above CAN evolve in future revisions. A host system MUST accept ProductFootprints from later revisions with `crossSectoralStandards` containing values that are not defined in this specification. */
  crossSectoralStandards: string[];
  /** The product-specific or sector-specific rules applied for calculating or allocating GHG emissions. If no product or sector specific rules were followed, this set MUST be empty. */
  productOrSectorSpecificRules?: ProductOrSectorSpecificRule[];
  /** The percentage of emissions excluded from the PCF. */
  exemptedEmissionsPercent: string;
  /** Rationale behind exclusion of specific PCF emissions, CAN be the empty string if no emissions were excluded. */
  exemptedEmissionsDescription?: string;
  /** Description of the allocation rules applied to the PCFs foreground data including an explanation of the underlying reasons (way of allocating all activities from manufacturing steps to the declared unit). */
  allocationRulesDescription?: string;
  /** If secondary data was used to calculate the <{CarbonFootprint}>, then it MUST include the property <{CarbonFootprint/secondaryEmissionFactorSources}> with value the emission factors used for the <{CarbonFootprint}> calculation. If no secondary data is used, this property MUST BE undefined. */
  secondaryEmissionFactorSources?: EmissionFactorSource[];
  /** Share of primary data in the final absolute PCF value excluding biogenic CO2 uptake (pcfExcludingBiogenicUptake). */
  primaryDataShare?: string;
  /** Data Quality Indicators (dqi) in accordance with the PACT Methodology. */
  dqi?: DataQualityIndicators;
  /** The presence of the <{Verification}> object indicates whether or not the <{CarbonFootprint}> has been verified in line with [=PACT Methodology=] requirements. */
  verification?: Verification;
}

/** Each data model extension MUST be a valid JSON object conforming with the [JSON Representation of a Data Model Extension](https://wbcsd.github.io/data-model-extensions/spec/#instantiation). See [[!DATA-MODEL-EXTENSIONS]] for technical details and [[!EXTENSIONS-GUIDANCE]] for data model extension guidance. */
export interface DataModelExtension {
  /** The version of the Data Model Extension specification. The value MUST be a string in the format major.minor.patch as defined in Semantic Versioning 2.0.0. */
  specVersion: string;
  /** The value MUST be the URL to the publicly accessible Extension Schema File */
  dataSchema: string;
  /** The value MUST be the URL to the publicly accessible Extension Documentation. */
  documentation?: string;
  /** The value MUST be a JSON Object that conforms to the extension schema file referenced by the dataSchema property. */
  data: Record<string, unknown>;
}

/** A ProductOrSectorSpecificRule refers to a set of product or sector specific rules published by a specific operator and applied during product carbon footprint calculation. */
export interface ProductOrSectorSpecificRule {
  /** Selection of operator of PCR being used for the PCF calculation. If operator is not available in the given list, or if a sector specific guidance has been followed, please set "Other" and include details under "otherOperatorName". */
  operator: ProductOrSectorSpecificRuleOperator;
  /** Names of the product or sector specific rules being used for the PCF calculation. */
  ruleNames: string[];
  /** If operator is Other, then this attribute must be populated with the name of the operator. */
  otherOperatorName?: string;
}

/** References emission factor databases, see [=PACT Methodology=] Section 4.1.3.2. ```json { "name": "ecoinvent", "version": "3.9.1" } ``` */
export interface EmissionFactorSource {
  /** Name of the secondary emission factor database */
  name: string;
  /** Version of the secondary emission factor database */
  version: string;
}

/** Data type DataQualityIndicators contains the quantitative data quality indicators. */
export interface DataQualityIndicators {
  /** Quantitative data quality rating (DQR) based on the data quality matrix, scoring the technological representativeness of the sources used for the final absolute PCF excluding biogenic CO2 uptake calculation based on weighted average of all inputs. The value MUST be between `1` and `5` inclusive. */
  technologicalDQR: string;
  /** Quantitative data quality rating (DQR) based on the data quality matrix, scoring the geographical representativeness of the sources used for the final absolute PCF excluding biogenic CO2 uptake calculation based on weighted average of all inputs. The value MUST be between `1` and `5` inclusive. */
  geographicalDQR: string;
  /** Quantitative data quality rating (DQR) based on the data quality matrix, scoring the temporal representativeness of the sources used for the final absolute PCF excluding biogenic CO2 uptake calculation based on weighted average of all inputs. The value MUST be between `1` and `5` inclusive. */
  temporalDQR: string;
}

/** Contains the verification in conformance with the PACT Methodology. */
export interface Verification {
  /** The coverage of the verification defines the type and level of GHG data to be verified. */
  coverage?: VerificationCoverage;
  /** The non-empty name of the independent third party engaged to undertake the verification. */
  providerName?: string;
  /** The date at which the verification was completed */
  completedAt?: string;
  /** Name of the standard against which the PCF was assured */
  standardName?: string;
  /** Any additional comments that will clarify the interpretation of the verification. */
  comments?: string;
}

/** Base class for all events, it follows the CloudEvents specification. */
export interface BaseEvent {
  /** Event type identifier. */
  type: string;
  /** CloudEvents version. */
  specversion: string;
  /** Event identifier. Must be able to uniquely identify the event by source and id. */
  id: string;
  /** Identifies the context in which an event happened. This can include information such as the domain, organization, application or process that produced the event. Source + id MUST be unique for each distinct event. The uri-reference */
  source: string;
  /** The time the event occurred. */
  time: string;
  /** The event payload. */
  data: Record<string, unknown>;
}

/** Notification that a ProductFootprint has been published (either new or updated). This event is triggered by the [=data owner=] and send to relevant [=data recipients=]. If the [=data recipient=] cannot be reached by the [=data owner=], the data owner SHOULD retry to send the event at a later time, using an exponential backoff strategy. It SHOULD abandon sending the event after 72 hours. The data object of the `PublishedEvent` contains the list of Product Footprint *IDs* that have been published or updated by the data owner. The data recipient can retrieve the *actual* Product Footprints by calling the [=Action GetFootprint=] action on the data owner's systems. */
export interface PublishedEvent extends BaseEvent {
  /** Event type identifier. */
  type: "org.wbcsd.pact.ProductFootprint.PublishedEvent.3";
  /** Object containing the list of Product Footprint Ids that have been published or updated by the data owner. ``` "data": { "pfIds": [ "uuid1", "uuid2", ... ] } ``` */
  data: Record<string, unknown>;
}

/** A data recipient requests a ProductFootprint from the data owner. The data recipient MUST provide the criteria in the data object to enable the data owner to either find an existing relevant PCF or create a new one. Criteria to provide can be product id, geography, company identifier, validity period, etc, see below for details. For filtering logic, see [[#filtering]]. If the data owner can *immediately* determine that it will not be able to handle this request it MUST respond with an appropriate `4xx` error response, otherwise it MUST respond with a `200` (Success), indicating it will be able to process the request asynchronously. Only in the latter case (after having accepted the request for processing) the data owner MUST send a follow-up event back to the data recipient at some time in the future. This follow up event MUST be either a `RequestFulfilledEvent` or `RequestRejectedEvent`. */
export interface RequestCreatedEvent extends BaseEvent {
  /** Event type identifier. */
  type: "org.wbcsd.pact.ProductFootprint.RequestCreatedEvent.3";
  /** Criteria for filtering ProductFootprint requests. */
  data: Record<string, unknown>;
}

/** Notification that the request for PCF(s) has been fulfilled by the data owner. This notification will be sent by the data owner back to the data recipient in response to a ProductFootprintRequest.Created event, see [[#request-created-event]]. The data object contains the original requestEventId and the resulting list of ProductFootprints which conform to the set of criteria specified by the data recipient in the preceding RequestCreated event. This list MUST be non-empty, as the RequestFulfilled event indicates success. The data owner MUST send a single RequestFulfilled event back to the data recipient per corresponding RequestCreated event. If there are no product footprints to return, the data owner MUST send a [[#request-rejected-event]] instead. If the [=data recipient=] cannot be reached by the [=data owner=], the data owner SHOULD retry to send the event at a later time, using an exponential backoff strategy. It SHOULD abandon sending the event after 72 hours. */
export interface RequestFulfilledEvent extends BaseEvent {
  /** Event type identifier. */
  type: "org.wbcsd.pact.ProductFootprint.RequestFulfilledEvent.3";
  /** data object */
  data: Record<string, unknown>;
}

/** Notification that a request for a ProductFootprint can not be fulfilled. The RequestRejectedEvent is an event sent back from the data owner to the data recipient upon NOT successfully fulfilling the preceding RequestCreatedEvent sent by the data recipient. If the [=data recipient=] cannot be reached by the [=data owner=], the data owner SHOULD retry to send the event at a later time, using an exponential backoff strategy. It SHOULD abandon sending the event after 72 hours. */
export interface RequestRejectedEvent extends BaseEvent {
  /** Event type identifier. */
  type: "org.wbcsd.pact.ProductFootprint.RequestRejectedEvent.3";
  /** data object */
  data: Record<string, unknown>;
}

/** Object with error code and description, to be returned by the API methods in case of error. See [[#api-error-handling]] for details. */
export interface Error {
  /** Error code identifier. */
  code: ErrorCode;
  /** Human readable error message. */
  message: string;
}
